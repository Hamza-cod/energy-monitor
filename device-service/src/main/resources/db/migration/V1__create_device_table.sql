CREATE TABLE IF NOT EXISTS devices (
                       id VARCHAR(255) PRIMARY KEY,
                       name VARCHAR(255),
                       location VARCHAR(255),
                       user_id VARCHAR(255)
);